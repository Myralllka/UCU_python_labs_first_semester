#include <stdio.h>

int main() {
    int n, m;
    int counter = 1;
    char *chr = "";
    int chr_counter = 0;
    
    for (int i = 0; i < n*m; i++) {
        if (counter > 99) {
            chr[chr_counter] = (char)counter / 10;

        }
    }


    scanf("%d %d", &n, &m);
    for (int i = 0; i < n; i++) {
        for (int j = 0; j<m; j++) {

        }
    }
    return 0;
}